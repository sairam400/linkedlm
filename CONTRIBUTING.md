# Contributing

1. Create a branch from `main`.
2. Run `npm run check` before opening a pull request.
3. Never commit `.env` or API keys.
4. Keep the browser usable without an API key by preserving demo mode.

Use conventional commit messages where practical, for example `feat: add draft history`.
